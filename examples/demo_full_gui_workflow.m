projectRoot=fileparts(fileparts(mfilename('fullpath')));originalDirectory=pwd;cleanup=onCleanup(@()cd(originalDirectory));cd(projectRoot);startup;cd(originalDirectory);
app=lmz.gui.LeggedModelZooApp();app.Controller.selectModel('slip_quadruped');solveResult=app.Controller.solveWorkingSolution(struct());app.Controller.makeSecondSeed(0.03);continuationResult=app.Controller.runContinuation(struct('MaximumPoints',5,'BothDirections',false));app.Controller.selectModel('slip_quad_load');optimizationResult=app.Controller.runOptimization(struct());delete(app);clear cleanup
