# Model template

These files are rendered by `tools/new_model.m`. Generated projects are
external and inactive until their root is explicitly passed to
`ModelRegistry.discoverWithPlugins`. Placeholder text is never evaluated.
